extends ../layouts/master.pug

block title
  title SMIT | Оборудование

block basicSeo
  meta(content="Оборудование" name="description")
  meta(content="Оборудование" name="keywords")

block manifestBrowserconfigFiles
  link(href="../manifest.json" rel="manifest" crossorigin)
  meta(content="../browserconfig.xml" name="msapplication-config")

block content
  main
    .barba(data-barba="wrapper")
      .barba-container(data-barba="container" data-barba-namespace="equipment")
        .page-container
          .page-wrapper
            include ../mixins/breadcrumbs.pug
            +breadcrumbs(
              [
                {
                  href: '',
                  title: 'Оборудование'
                }
              ]
            )
            include ../components/works.pug
            include ../components/clients.pug
          include ../components/footer.pug